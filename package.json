{
  "name": "sass",
  "version": "1.0.0",
  "description": "trabajo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile:sass": "sass --watch scss:css"
  },
  "author": "eduardo",
  "license": "ISC",
  "dependencies": {
    "boostrap": "^2.0.0",
    "bootstrap": "^4.6.0"
  }
}
